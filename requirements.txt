fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.3
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1
python-multipart==0.0.20

# Auth: JWT tokens + bcrypt password hashing
PyJWT==2.9.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# AI assistant (RAG QnA). Embeddings are pure-numpy feature-hashing (no extra deps);
# only the LLM call needs langchain-groq.
langchain-groq>=0.2.0

# AI bill extraction (purchase "Upload Bill"): Mistral OCR + LLM over PDFs/images.
httpx>=0.27.0
PyPDF2>=3.0.0
pdfplumber>=0.11.0
